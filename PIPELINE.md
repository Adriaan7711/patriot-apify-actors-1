# Moved

The Clay and Monday handoff is documented once for all four Actors, in
`../PIPELINE.md` at the root of this bundle. This Actor's dataset feeds the same
three Clay tables described there, and its own schedule is the first two rows of
that document's schedule table.
